kind = "api"
previewPath = "/api" # TODO - should be excluded from preview in the first place
title = "API Server"
version = "1.0.0"
id = "3B4_FFSkEVBkAeYMFRJ2e"

[[services]]
localPort = 8080
name = "API Server"
paths = ["/api"]

[services.development]
run = "cd ../.. && python3 -m uvicorn hamad_ai_api.main:app --host 0.0.0.0 --port ${PORT:-8080}"

[services.production]

[services.production.build]
args = ["python3", "-m", "compileall", "hamad_ai", "hamad_ai_api"]

[services.production.build.env]
NODE_ENV = "production"

[services.production.run]
# we don't run through pnpm to make startup faster in production
args = ["python3", "-m", "uvicorn", "hamad_ai_api.main:app", "--host", "0.0.0.0", "--port", "8080"]

[services.production.run.env]
PORT = "8080"
NODE_ENV = "production"

[services.production.health.startup]
path = "/api/healthz"
